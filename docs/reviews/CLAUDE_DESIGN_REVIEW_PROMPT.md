# Независимый read-only аудит AI Wardrobe

Работай только как независимый reviewer. Не изменяй файлы, не запускай сервисы и не обращайся к секретам.

Прочитай `CLAUDE.md`, `PROJECT_MASTER_PROMPT.md`, `IMPROVEMENTS.md`, React-код в
`apps/miniapp/src`, API-модули аватара/биллинга/загрузок, модели SQLAlchemy,
миграции `0002` и `0003`, а также тесты новых функций.

Проведи четыре проверки:

1. UX и визуальная архитектура всех вкладок Telegram Mini App, особенно редактора
   области, тарифов, промокода, персонального аватара и virtual try-on.
2. Техническая архитектура, приватность, безопасность, производительность и
   коммерческая готовность.
3. Нормализация БД до 3НФ/BCNF: укажи только доказанные нарушения или риски.
4. Критические дефекты реализации с точными путями и строками.

Отделяй факты из кода от предположений. Не утверждай, что генерация физически точно
предсказывает посадку или размеры. В конце дай:

- verdict `ship / conditional ship / do not ship`;
- пять приоритетных доработок P0/P1/P2;
- отдельное мнение, нужен ли перенос на Go на основании найденных узких мест;
- краткую оценку дизайна по десятибалльной шкале.
