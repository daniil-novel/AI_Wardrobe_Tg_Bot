# Technical Debt

- Replace contract-safe stub responses with persistent service implementations per module.
- Add webhook mode for production bot deployment.
- Add OpenRouter cost persistence into `ai_requests`.
- Add real marketplace provider adapters and trust-layer checks.
- Add rate limiting middleware backed by Redis.
- Add admin dashboard routes and RBAC-protected UI.
- Add frontend API client with authenticated Telegram initData exchange.
- Add Playwright visual checks for Telegram WebView viewports.
